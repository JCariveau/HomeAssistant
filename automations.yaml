- alias: "Power state on HA start-up"
  id: power_startup
  hide_entity: True
  trigger:
    platform: homeassistant
    event: start
  action:
    - service: mqtt.publish
      data:
        topic: "cmnd/sonoffs/power"
        payload: ""
